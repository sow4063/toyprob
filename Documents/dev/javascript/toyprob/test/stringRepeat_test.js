var assert = require('chai').assert;

describe("Solution", function(){
  it("should test for something", function(){
    var str = 'a';
    assert.equal( str.repeat(3), 'aaa', "This is just an example of how you can write your own TDD tests");
  });
});
